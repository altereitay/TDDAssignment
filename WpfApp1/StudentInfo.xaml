<Page x:Class="WpfApp1.StudentInfo"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="StudentInfo">

    <Grid>
        <ListView x:Name="Student_Info" Background="Transparent">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="First Name" DisplayMemberBinding="{Binding [0]}" />
                    <GridViewColumn Header="Last Name" DisplayMemberBinding="{Binding [1]}" />
                    <GridViewColumn Header="ID" DisplayMemberBinding="{Binding [2]}" />
                    <GridViewColumn Header="Email" DisplayMemberBinding="{Binding [3]}" />
                    <GridViewColumn Header="Average" DisplayMemberBinding="{Binding [4]}" />
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Page>
